
<body style="background-color: #bb0a21">
    <div classname="row">
        <div class="card card__w mb-4 mt-5" style="background-color: #F9BF8F">
            <div class="card-header">Haberi Düzenle</div>
            <div class="card-body">
                <form action="/admin/posts/{{post.id}}?_method=PUT" method="post" enctype="multipart/form-data">
                <input type="hidden" name="_method" value="PUT">
                    <div class="row">
                        <div class="col-md-12">
                            <div class="form-group">
                                <label class="small mb-1 mr-3">Yüklenicek Kapak Fotorafını Seç</label>
                                <input class="form-control" name="post_image" type="file" required="required">
                                <p class="help-block text-danger"></p>
                            </div>
                            <div class="form-group">
                                <label class="small mb-1">Haberin Başlığı</label>
                                <input class="form-control" name="title" type="text" value="{{post.title}}"
                                    placeholder="Haberin Başlığını Girin" required="required">
                                <p class="help-block text-danger"></p>
                            </div>

                            <div class="form-group">
                                <label class="small mb-1">Kategori Seçin</label>
                                <select class="form-control" name="category">
                                    {{#each categories}}
                                    <option value="{{id}}">{{name}}</option>
                                    {{/each}}
                                </select>
                            </div>
                            <div class="form-group">

                                    <h1 class="h1_comment_">Haberin İçeriği</h1>
                                    <textarea class="form-control mt-3 txta" rows="10" cols="80" name="content"
                                        placeholder="Haberin İçeriğini Girin" required="required">{{post.content}}</textarea>
                                    <p class="help-block text-danger"></p>

                            </div>
                        </div>
                        <div class="clearfix"></div>
                        <div class="col-lg-12 text-center">
                            <div id="success"></div>
                            <button type="submit" class="btn btn-primary mr-5">Güncelle</button>
                            <a href="/"> <button type="button"
                            class="btn btn-primary">İptal</button></a>
                        </div>
                    </div>
                </form>
            </div>
        </div>

    </div>